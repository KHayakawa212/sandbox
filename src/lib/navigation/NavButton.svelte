<script lang="ts">
	//TODO isActive should be reactive
	export let isActive: boolean;
	const navButtonClasses = isActive ? '' : '';
	console.log(navButtonClasses);
	// TODO checkout cva and tailwind-merge to set components variants
</script>

<div></div>
